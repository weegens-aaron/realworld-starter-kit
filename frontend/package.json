{
  "name": "conduit-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Conduit frontend static assets (vanilla JS, no bundler).",
  "type": "commonjs",
  "scripts": {
    "test": "node --test"
  }
}
